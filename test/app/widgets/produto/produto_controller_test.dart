import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:uimanager/app/widgets/produto/produto_controller.dart';
import 'package:uimanager/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ProdutoController produto;
  //
  setUp(() {
    //     produto = AppModule.to.get<ProdutoController>();
  });

  group('ProdutoController Test', () {
    //   test("First Test", () {
    //     expect(produto, isInstanceOf<ProdutoController>());
    //   });

    //   test("Set Value", () {
    //     expect(produto.value, equals(0));
    //     produto.increment();
    //     expect(produto.value, equals(1));
    //   });
  });
}
